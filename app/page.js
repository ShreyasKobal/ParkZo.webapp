export default function Home() {
  return (
    <main>
      <p>ParkZo Next.js scaffold — page migration not started yet.</p>
      <p>
        The current site remains available at the existing static HTML pages
        (e.g. <code>index.html</code>) until Phase 1 migration is complete.
      </p>
    </main>
  );
}
